{
  "name": "tabr",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && uvicorn main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
